@using Microsoft.Extensions.Configuration
@inject IConfiguration _configuration


<header>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
        <div class="container">
            <a href="/" class="navbar-brand">
                <img src="~/images/logo.jpg" width="30" height="30" alt="logo image" asp-append-version="true">@_configuration["AppName"]
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-collapse" aria-controls="navbar-collapse" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse d-sm-inline-flex flex-sm-row-reverse">
                <ul class="navbar-nav">
                    @* <li class="nav-item">
                        <a class="btn btn-outline-primary" asp-controller="Account" asp-action="SignIn">Login</a>
                        <a class="btn btn-primary" asp-controller="Account" asp-action="Signup">Register</a>
                    </li> *@
                    <partial name="_LoginInfo"/>
                </ul>
                <ul class="navbar-nav flex-grow-1">
                    <li class="nav-item">
                        @* href="/" *@
                        <a class="nav-link text-dark" asp-controller="Home" asp-action="Index">Home</a>
                    </li>
                    <li class="nav-item">
                        @* href="/home/aboutus"  *@
                        <a class="nav-link text-dark" asp-controller="Home" asp-action="aboutus">About</a>
                    </li>
                    <li class="nav-item">
                        @* href="/home/aboutus" *@
                        <a class="nav-link text-dark" asp-controller="Home" asp-action="contactus">Contact</a>
                    </li>
                    <li class="nav-item">
                        @* href="/book/getallbooks" *@
                        <a class="nav-link text-dark" asp-controller="Book" asp-action="getallbooks" \
                        >AllBooks</a>
                    </li>
                        @* asp-protocol="http" asp-host="webgentle.com" *@
                    <li class="nav-item">
                        @* href="/book/getallbooks" *@
                        <a class="nav-link text-dark" asp-controller="Book" asp-action="addnewbook" \
                        >add_Book</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    @* <div class="alert alert-info" role="alert">
        @(_configuration.GetValue<string>("fromappsettings"))
    </div> *@
</header>